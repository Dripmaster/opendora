{
  "name": "opendora",
  "version": "0.2.0",
  "private": true,
  "description": "Python-based Discord Codex orchestrator",
  "scripts": {
    "dev": "uv --directory apps/orchestrator_py run orchestrator",
    "start": "uv --directory apps/orchestrator_py run orchestrator",
    "test": "uv --directory apps/orchestrator_py run --extra dev pytest -q",
    "check": "uv --directory apps/orchestrator_py run --extra dev pytest -q"
  }
}
